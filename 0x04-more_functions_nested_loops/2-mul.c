#include "main.h"
/**
 * mul - Entry point
 * @a: The character to be checked
 * @b: The character to be checked
 *
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	multiply = a * b;
	{
		_putchar(multiply);
	}
	return (0);
}
