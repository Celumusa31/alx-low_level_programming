#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _memcpy - Entry point
 * @dest: Character to be checked
 * @src: Character to be checked
 * @n: Character to be checked
 *
 * Return: Always pointer (Success)
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	memcpy(dest; src; sizeof(src) + n);
	return (dest);
}
