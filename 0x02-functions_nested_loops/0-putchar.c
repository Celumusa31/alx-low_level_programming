#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c[] = "_putchar";

	for (int = i = 0; i < sizeof(c); i++)
	{
		putchar(c[i]);
	}
	return (0);
}
