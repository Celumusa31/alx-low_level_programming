#include "main.h"
#include <stdio.h>
/**
 * factorial - Entry point
 * @n: Character to be checked
 *
 * Return: Always 0 and -1 (Success)
 */
int factorial(int n)
{
	int fact = 1;
	int i = 0;

	if (i <= n)
	{
		fact = fact * i;
		i++;
	}
	return (fact);

	else if
	{
		return (-1);

	}
}
