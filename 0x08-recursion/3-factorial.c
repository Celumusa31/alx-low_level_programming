#include "main.h"
#include <stdio.h>
/**
 * factorial - Entry point
 * @n: Character to be checked
 *
 * Return: Always 0 and -1 (Success)
 */
int factorial(int n)
{
	if (y == 0)
		return 1;

	return y * factorial(y - 1)
}
