#include "main.h"
#include <stdio.h>

/**
 * print_rev - Entry point
 * @s: Character to be checked
 *
 * Return: Always void (Success)
 */
void print_rev(char *s)
{
	reverseString(s);
	S = write(s);
}
