#include "main.h"

/**
 * _strlen - Entry point
 * @s: Character to be checked
 *
 * Return: Always integer (Success)
 */
int _strlen(char *s)
{
	int strlen = 0;

	while(*c != '\0')
	{
		strlen++;
		c++;
	}
	return (strlen);
}
